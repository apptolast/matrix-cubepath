services:
  app:
    build: .
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3939
      DATA_DIR: /data
      SESSION_SECRET: ${SESSION_SECRET}
    volumes:
      - matrix_data:/data
    ports:
      - '3939:3939'

volumes:
  matrix_data:
